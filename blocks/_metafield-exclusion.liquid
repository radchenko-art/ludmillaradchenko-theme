{% comment %}
  Block only used for settings (excluded metafield key). No output.
{% endcomment %}

{% schema %}
{
  "name": "t:names.metafield_exclusion",
  "tag": null,
  "settings": [
    {
      "type": "text",
      "id": "key",
      "label": "t:settings.metafield_key"
    }
  ]
}
{% endschema %}
